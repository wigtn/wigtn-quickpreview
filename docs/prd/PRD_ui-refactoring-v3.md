# WIGVU UI Refactoring v3 — Liner Hero + Feature 통합

> **Version**: 3.0
> **Created**: 2026-02-13
> **Status**: Draft
> **Reference**: Liner (liner.com) — 사이드바 + 중앙 인풋 Hero 패턴
> **Predecessor**: PRD_ui-overhaul.md, PRD_korean-study.md, DIGGING_ui-refactoring-v3.md

---

## 1. Overview

### 1.1 Problem Statement

현재 WIGVU에는 3가지 구조적 문제가 있다:

1. **기능 중복**: Study(`/` → `/study`)와 Article(`/read`)이 거의 동일한 기능을 제공하면서 분리되어 있다. 둘 다 텍스트/URL 입력 → AI 문장별 번역 + 표현 추출을 하지만, Article이 문장 구조 파싱과 단어 팝오버까지 지원하는 **상위호환**이다.

2. **첫 화면 미스매치**: 현재 홈페이지는 텍스트 입력 필드가 바로 나오는데, 사용자가 "무엇을 학습할지 선택"하는 경험이 없다. 첫 방문자에게 빈 입력창만 보이면 서비스의 가치를 즉시 이해하기 어렵다.

3. **사이드바 혼란**: "Read"와 "Articles"라는 두 메뉴가 존재하는데 차이가 불분명하다. Liner처럼 각 메뉴가 명확히 다른 도구를 제공해야 한다.

### 1.2 Goals

| ID | Goal | 측정 기준 |
|----|------|----------|
| G1 | Study/Article을 하나의 "Read" 기능으로 통합 | 사이드바 메뉴 2개 → 1개 |
| G2 | 홈페이지에서 학습 모드 선택 + 추천 콘텐츠 카드 그리드 | 첫 화면에서 콘텐츠 선택 가능 |
| G3 | Liner Hero 패턴 적용 (헤드라인 + 중앙 인풋) | Liner 홈페이지와 동일한 구조 |
| G4 | 사이드바 정리: Read + Watch 명확한 2개 도구 | 메뉴 혼란 제거 |
| G5 | 큐레이션된 학습 콘텐츠 카드 15-25개 | 첫 방문자도 바로 학습 시작 가능 |

### 1.3 Non-Goals (Out of Scope)

- 회원가입/로그인 시스템
- 학습 진도 저장 (DB 필요, 추후)
- 온보딩 모달 (Phase 2에서)
- 동영상 분석 기능 자체의 변경 (UI 배치만)
- 다크모드 컬러 시스템 변경 (현재 유지)
- 결제 시스템 변경
- AI 백엔드 변경

### 1.4 Scope

| 포함 | 제외 |
|------|------|
| Study/Article 기능 통합 (코드 삭제+병합) | 백엔드 AI API 변경 |
| 홈페이지 재설계 (Liner Hero + 카드 그리드) | 새로운 AI 기능 추가 |
| 사이드바 메뉴 구조 변경 | 데이터베이스 도입 |
| 큐레이션 콘텐츠 데이터 (정적 JSON) | 인증/인가 시스템 |
| 라우팅 구조 변경 | 결제 시스템 |
| TopBar 역할 정리 | 모바일 네이티브 앱 |

---

## 2. Design Direction

### 2.1 Liner 레퍼런스 핵심 패턴

Liner 스크린샷 4장에서 추출한 적용 대상 패턴:

| Liner 패턴 | WIGVU 적용 |
|-----------|-----------|
| 사이드바에서 도구 선택 → 메인 영역 변경 | Read/Watch 전환 시 히어로+인풋 변경 |
| 중앙 정렬 헤드라인 + 인풋 카드 | 홈페이지 Hero 섹션 |
| 인풋 하단에 프로모/콘텐츠 영역 | 추천 콘텐츠 카드 그리드 |
| TopBar에 인증 버튼만 | Pricing + Language + Login |
| 접힌 사이드바 → 아이콘만 | 현재 구현 유지 |
| 미니 푸터 (요금제·한국어·팀 플랜) | 향후 고려 |

### 2.2 Design Principles

```
1. Content-First     — 첫 화면에서 바로 학습할 콘텐츠를 선택 가능
2. Tool Clarity      — Read와 Watch, 두 도구의 차이가 즉시 인식 가능
3. Liner Consistency — 사이드바 + 중앙 인풋 + 카드 하단의 Liner 패턴 유지
4. Zero Friction     — 첫 방문자도 카드 클릭 한 번으로 학습 시작
5. Unified Feature   — 중복 기능 없이 하나의 통합된 학습 경험
```

### 2.3 Color System

**변경 없음** — 현재 Blue 액센트 유지.

| Token | Light | Dark |
|-------|-------|------|
| `--accent` | `#2563EB` | `#3B82F6` |
| `--accent-light` | `#EFF6FF` | `#1E293B` |

나머지 컬러 토큰은 현재 `globals.css` 유지.

### 2.4 Typography & Spacing

현재 시스템 유지. 추가 토큰 없음.

---

## 3. Information Architecture

### 3.1 라우팅 구조 (변경)

```
현재:
/              → 텍스트 인풋 (StudyInput + RecommendedLinks)
/study         → 학습 결과 (SentenceView + ExpressionCard)
/read          → 기사 분석 (ArticlePanel + ExpressionBar)
/video         → YouTube 인풋
/analyze/[id]  → 영상 분석 결과
/pricing       → 요금제
/payment/*     → 결제 콜백

변경 후:
/              → 홈페이지 (Liner Hero 인풋 + 추천 카드 그리드)
/read          → 읽기 학습 결과 (통합 ArticlePanel — 기존 /read 유지)
/video         → YouTube 인풋 (변경 없음)
/analyze/[id]  → 영상 분석 결과 (변경 없음)
/pricing       → 요금제 (변경 없음)
/payment/*     → 결제 콜백 (변경 없음)

삭제:
/study         → /read로 통합 (삭제)
```

### 3.2 네비게이션 — 사이드바

**변경 전:**
```
── Learn ──
📖 Read        → /       (텍스트 인풋)
📰 Articles    → /read   (URL/텍스트 인풋)

── Tools ──
🎬 Video Analysis → /video
```

**변경 후:**
```
── Learn ──
📖 Read        → /       (홈 — Liner Hero + 카드 그리드)
🎬 Watch       → /video  (YouTube 인풋)
```

**변경 사항:**
- "Articles" 항목 **삭제** (Read에 통합)
- "Video Analysis" → "Watch" (라벨 단순화)
- "Tools" 섹션 삭제 → "Learn" 하나로 통합

**활성 조건:**

| 항목 | 아이콘 | 라우트 | 활성 조건 |
|------|--------|--------|----------|
| Read | `BookOpen` | `/` | `pathname === "/"` 또는 `/read` |
| Watch | `Film` | `/video` | `pathname === "/video"` 또는 `/analyze/*` |

### 3.3 TopBar (변경 최소)

현재 유지:
```
                              Pricing  Language∨  Login
```

Language 선택기는 TopBar에 유지 (사이드바에서 제거된 상태 유지).

---

## 4. Page Designs

### 4.1 홈페이지 — `/` (Liner Hero + 카드 그리드)

```
┌─────────┬──────────────────────────────────────────┐
│         │              (상단 여백 ~10vh)            │
│ Sidebar │                                          │
│         │      Learn Korean Your Way               │  ← H1 (24px, 볼드, 중앙)
│ 📖 Read │      Read real Korean articles with       │  ← Body Small (14px, gray, 중앙)
│  (활성)  │      AI-powered translations              │
│         │                                          │
│ 🎬 Watch│  ┌──────────────────────────────────┐    │
│         │  │                                  │    │
│         │  │  Paste Korean text or URL...      │    │  ← 인풋 카드
│         │  │                                  │    │     border: 1px --border
│         │  │                        [Start →] │    │     border-radius: 12px
│         │  └──────────────────────────────────┘    │
│         │                                          │
│ ── ── ──│  Recommended for you                     │  ← 섹션 레이블 (12px, gray)
│ ☀️/🌙  │                                          │
│ GitHub  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│         │  │ Beginner │ │ Beginner │ │ Inter.   │ │  ← 카드 그리드
│         │  │          │ │          │ │          │ │     3열 (데스크탑)
│         │  │ 한국의   │ │ 서울의   │ │ 한국어   │ │     2열 (태블릿)
│         │  │ 커피 문화 │ │ 대중교통 │ │ 존댓말   │ │     1열 (모바일)
│         │  │ · Culture │ │ · Daily  │ │ · Culture│ │
│         │  │ 5 min     │ │ 4 min    │ │ 6 min    │ │
│         │  │ → Read    │ │ → Read   │ │ → Read   │ │
│         │  └──────────┘ └──────────┘ └──────────┘ │
│         │                                          │
│         │  ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│         │  │ Beginner │ │ Advanced │ │ Inter.   │ │
│         │  │ ...      │ │ ...      │ │ ...      │ │
│         │  └──────────┘ └──────────┘ └──────────┘ │
│         │                                          │
└─────────┴──────────────────────────────────────────┘
```

**인풋 영역 (Liner 검색창 패턴):**
- `max-width: 640px`, 중앙 정렬
- `border: 1px solid var(--border)`, `border-radius: 12px`
- `padding: 16px 20px`
- 그림자 없음, 배경 `var(--surface)`
- focus 시: `border-color: var(--accent)`
- 우하단: [Start →] 버튼 (`var(--accent)` 컬러)
- URL 패턴(`http(s)://`) 감지 시 "Text will be fetched from URL" 안내
- 텍스트에어리어: `min-height: 80px` (현재 StudyInput 대비 약간 축소)
- Enter 또는 Cmd+Enter로 제출

**인풋 제출 동작:**
- URL 입력 → `/read?url={encoded_url}` 이동
- 텍스트 입력 → `sessionStorage.readText`에 저장 → `/read` 이동

**추천 카드 그리드:**
- 인풋 아래 `margin-top: 32px`
- "Recommended for you" 섹션 레이블 (12px, `--foreground-secondary`)
- 카드 그리드: `grid-cols-3` (데스크탑), `grid-cols-2` (태블릿), `grid-cols-1` (모바일)
- `max-width: 960px`, 중앙 정렬
- 카드 클릭 → `/read?article={articleId}` 이동

**카드 디자인:**

```
┌──────────────────────┐
│ Beginner    · Culture │  ← 난이도 배지 (Level Color) + 카테고리
│                       │
│ 한국의 커피 문화,      │  ← 제목 (16px, bold, 2줄 max)
│ 세계 3위 소비국의 비밀  │
│                       │
│ 한국은 하루에           │  ← 미리보기 (14px, gray, 2줄 max, ellipsis)
│ 약 2,600만 잔의...     │
│                       │
│ 5 min read  → Read    │  ← 읽기 시간 + CTA
└──────────────────────┘
```

| 카드 요소 | 스타일 |
|----------|--------|
| 컨테이너 | `bg: var(--card)`, `border: 1px solid var(--border)`, `border-radius: 8px`, `padding: 16px` |
| 난이도 배지 | `font-size: 12px`, `font-weight: 500`, Level Color (Beginner=Green, Intermediate=Amber, Advanced=Red) |
| 카테고리 | `font-size: 12px`, `color: var(--foreground-secondary)` |
| 제목 | `font-size: 16px`, `font-weight: 600`, `line-clamp: 2` |
| 미리보기 | `font-size: 14px`, `color: var(--foreground-secondary)`, `line-clamp: 2` |
| CTA | `font-size: 14px`, `color: var(--accent)`, `font-weight: 500` |
| 호버 | `border-color: var(--accent)`, `shadow-sm` |

### 4.2 읽기 학습 결과 — `/read` (통합)

기존 `/read` 페이지를 기반으로, `/study`의 유용한 기능을 흡수한다.

**입력 방식 3가지:**

| 입력 경로 | URL 파라미터 | 동작 |
|----------|-------------|------|
| 인풋에 텍스트 입력 | 없음, `sessionStorage.readText` | 텍스트 직접 분석 |
| 인풋에 URL 입력 | `?url={encoded}` | URL에서 크롤링 후 분석 |
| 추천 카드 클릭 | `?article={articleId}` | 정적 데이터에서 텍스트 로드 후 분석 |

**결과 뷰 구조 (기존 Article + Study 병합):**

```
┌─────────┬──────────────────────────────────────────┐
│         │  [← Back]  기사 제목 (또는 "Article")     │  ← 헤더
│ Sidebar │  KBS · 2026-02-13                        │
│         │                                          │
│ 📖 Read │  📌 Key Expressions (8)                  │  ← ExpressionBar (Article)
│  (활성)  │  [기준금리] [동결하다] [물가 상승] ...     │
│         │                                          │
│ 🎬 Watch│  [Both] [Original] [Translation]         │  ← DisplayMode 토글 (Article)
│         │                                          │
│         │  한국은행은 오늘 기준금리를                 │  ← ArticlePanel (Article)
│         │  연 3.5%로 동결했다.                      │     + 문장번호 + Parse 버튼
│         │  The Bank of Korea froze the base    [🔍]│
│         │  rate at 3.5% per year today.            │
│         │  ─────────────────────────────────────   │
│         │  이번 결정은 최근 물가 상승세가             │
│         │  둔화된 점을 감안한 것으로 분석된다.        │
│         │  This decision is analyzed as having [🔍]│
│         │  taken into account the recent...        │
│         │                                          │
│         │  ── 🔍 Structure Analysis ──────────     │  ← SentenceParser (Article)
│         │  │ [한국은행은]   Subject   Bank of Korea │
│         │  │ [기준금리를]   Object    base rate    │
│         │  │ [동결했다]    Predicate  froze        │
│         │  └───────────────────────────────────    │
│         │                                          │
│         │  ── Expression Summary ──────────────    │  ← ExpressionSummary (Study에서 흡수)
│         │                                          │
│         │  Idiom (2)                               │
│         │  · 기준금리를 동결하다 — to freeze...     │
│         │  · 물가 상승세 — price increase trend    │
│         │                                          │
│         │  Grammar Pattern (1)                     │
│         │  · -(으)ㄴ 것으로 분석된다 — analyzed as  │
│         │                                          │
│         │  [← New Article]                         │  ← 홈으로 돌아가기
│         │                                          │
└─────────┴──────────────────────────────────────────┘
```

**Study에서 흡수하는 기능:**

| 기능 | 원본 | 통합 방법 |
|------|------|----------|
| ExpressionSummary | `features/study/components/expression-summary.tsx` | `/read` 결과 하단에 추가 (스크롤 끝) |
| ViewModeToggle 로직 | `features/study/components/view-mode-toggle.tsx` | ArticlePanel의 기존 displayMode 토글로 대체 (이미 동일) |
| "New Article" 버튼 | Study 하단의 "새 글 학습하기" | `/read` 결과 하단에 [← New Article] 버튼 → `/` 이동 |

**Article 기존 기능 유지:**
- ExpressionBar (가로 스크롤 표현 바)
- ArticlePanel (문장 번호 + displayMode 토글 + Parse 버튼)
- SentenceParser (문장 구조 분석)
- SelectionPopover (단어 선택 팝오버)
- SSE 스트리밍 로딩 (3단계 진행률)

### 4.3 영상 분석 — `/video` (변경 없음)

레이아웃, 기능 변경 없음. 사이드바 "Watch" 항목 활성만 변경.

### 4.4 영상 분석 결과 — `/analyze/[videoId]` (변경 없음)

변경 없음.

### 4.5 요금제 — `/pricing` (변경 없음)

변경 없음.

### 4.6 모바일 레이아웃

모바일 변경사항:

```
┌──────────────────────────────┐
│ [☰]  WIGVU             [🌐] │  ← 모바일 상단 바 (변경 없음)
├──────────────────────────────┤
│                              │
│  Learn Korean Your Way       │
│  Read real Korean articles   │
│  with AI-powered translations│
│                              │
│  ┌──────────────────────┐   │
│  │ Paste Korean text     │   │
│  │ or URL...             │   │
│  │              [Start →]│   │
│  └──────────────────────┘   │
│                              │
│  Recommended for you         │
│  ┌──────────────────────┐   │  ← 1열 카드
│  │ Beginner · Culture    │   │
│  │ 한국의 커피 문화       │   │
│  │ 한국은 하루에...       │   │
│  │ 5 min       → Read    │   │
│  └──────────────────────┘   │
│  ┌──────────────────────┐   │
│  │ Beginner · Daily      │   │
│  │ 서울 지하철 이용 가이드│   │
│  │ ...                   │   │
│  └──────────────────────┘   │
│                              │
└──────────────────────────────┘
```

모바일 햄버거 오버레이:
```
┌──────────────┬───────────────┐
│ 🇰🇷 WIGVU [✕] │               │
│              │               │
│ ── Learn ──  │  (딤 배경)     │
│ 📖 Read      │               │
│ 🎬 Watch     │               │
│              │               │
│ ── ── ── ── │               │
│ ☀️ Light     │               │
│ GitHub ↗    │               │
└──────────────┴───────────────┘
```

---

## 5. 큐레이션 콘텐츠 데이터

### 5.1 콘텐츠 타입 정의

```typescript
interface CuratedArticle {
  id: string;                    // 고유 ID (e.g., "korean-coffee-culture")
  title: string;                 // 한국어 제목
  source: string;                // 출처 (e.g., "자체 제작", "공공누리")
  category: ArticleCategory;     // 카테고리
  difficulty: ArticleDifficulty; // 난이도
  readingTimeMinutes: number;    // 예상 읽기 시간
  preview: string;               // 미리보기 텍스트 (2줄, ~60자)
  content: string;               // 한국어 본문 전체
}

type ArticleCategory = "daily" | "culture" | "society" | "economy" | "technology";
type ArticleDifficulty = "beginner" | "intermediate" | "advanced";
```

### 5.2 카테고리 라벨

| Key | 영어 라벨 |
|-----|----------|
| daily | Daily Life |
| culture | Culture |
| society | Society |
| economy | Economy |
| technology | Technology |

### 5.3 콘텐츠 구성 (15-25개)

모든 콘텐츠는 **자체 제작** 또는 **공공누리 유형1(출처표시)** 콘텐츠.
시의성 낮은 에버그린 콘텐츠 위주.

| 카테고리 | 예시 제목 | 난이도 | 수량 |
|---------|----------|--------|------|
| Daily Life | 한국의 커피 문화, 대중교통 이용 가이드, 한국의 사계절, 편의점 문화, 배달 앱 이야기 | Beginner | 5-7 |
| Culture | K-드라마로 배우는 표현, 한국어 존댓말의 이해, K-pop 팬 문화, 한국의 명절 | Beginner~Intermediate | 4-6 |
| Society | 한국의 교육 열풍, 저출산 문제, MZ세대 문화 | Intermediate | 3-4 |
| Economy | 한국 반도체 산업, 부동산 시장 | Intermediate~Advanced | 2-3 |
| Technology | AI 시대의 한국어, 디지털 정부 | Advanced | 2-3 |

**저작권 전략:**
- "뉴스 기사" 대신 **"학습 텍스트"**로 포지셔닝
- Phase 1: 자체 제작 (GPT로 초안 → 수동 편집) + 공공누리 콘텐츠
- 기사 본문은 서버에 캐싱하지 않음 (정적 JSON으로 번들에 포함)

### 5.4 데이터 파일 위치

```
features/article/data/curated-articles.ts   ← 정적 CuratedArticle[] export
```

---

## 6. Feature 통합 상세

### 6.1 삭제 대상 (Study 피처)

| 파일 | 이유 |
|------|------|
| `features/study/components/study-input.tsx` | 홈페이지 인풋으로 대체 |
| `features/study/components/sentence-view.tsx` | ArticlePanel로 대체 |
| `features/study/components/view-mode-toggle.tsx` | ArticlePanel의 displayMode 토글로 대체 |
| `features/study/components/expression-card.tsx` | ExpressionBar + ExpressionSummary로 대체 |
| `features/study/components/recommended-links.tsx` | 카드 그리드로 대체 |
| `features/study/hooks/use-study-analysis.ts` | useArticleAnalysis로 대체 |
| `features/study/services/study.ts` | Article 서비스로 대체 |
| `features/study/types/study.ts` | Article 타입으로 통합 |
| `app/study/page.tsx` | `/read`로 통합 |
| `app/api/study/analyze/route.ts` | `/api/article/stream`으로 대체 |

### 6.2 Study → Article 흡수 기능

| Study 기능 | Article에 추가 방법 |
|-----------|-------------------|
| ExpressionSummary | `features/article/components/expression-summary.tsx` 신규 생성 (Study 코드 이식) |
| "New Article" 버튼 | `/read` 결과 하단에 `[← New Article]` 버튼 추가 |
| ExpressionCard 카테고리 | Article types에 `grammar_pattern`, `formal_expression`, `slang` 추가 |

### 6.3 Article 타입 확장

```typescript
// 기존 ArticleExpression.category
type ExpressionCategory = "idiom" | "phrasal_verb" | "collocation" | "technical_term";

// 확장 (Study 카테고리 흡수)
type ExpressionCategory =
  | "idiom"
  | "phrasal_verb"
  | "collocation"
  | "technical_term"
  | "grammar_pattern"      // Study에서 흡수
  | "formal_expression"    // Study에서 흡수
  | "slang";               // Study에서 흡수
```

### 6.4 홈페이지 인풋 → `/read` 데이터 전달

| 입력 경로 | 전달 방식 | `/read` 동작 |
|----------|----------|-------------|
| 텍스트 입력 | `sessionStorage.setItem("readText", text)` → navigate `/read` | `sessionStorage`에서 텍스트 읽기 → `startAnalysis({ text })` |
| URL 입력 | navigate `/read?url={encoded}` | URL 파라미터 읽기 → `startAnalysis({ url })` |
| 카드 클릭 | navigate `/read?article={articleId}` | 정적 데이터에서 article 조회 → `startAnalysis({ text: article.content, title: article.title })` |

---

## 7. User Stories

### US-01: 추천 카드에서 학습 시작

```gherkin
Scenario: 첫 방문자가 추천 글 카드를 클릭하여 학습을 시작한다
  Given 사용자가 WIGVU 홈페이지에 접속한다
  When 페이지가 로드된다
  Then 상단에 헤드라인과 텍스트 인풋이 보인다
  And 인풋 아래에 "Recommended for you" 카드 그리드가 보인다
  When "한국의 커피 문화" (Beginner · Culture) 카드를 클릭한다
  Then /read?article=korean-coffee-culture 페이지로 이동한다
  And 해당 글의 문장별 번역과 표현이 표시된다
```

### US-02: 직접 텍스트 입력으로 학습

```gherkin
Scenario: 사용자가 직접 한국어 텍스트를 붙여넣어 학습한다
  Given 홈페이지 인풋이 보인다
  When 한국어 텍스트를 붙여넣고 [Start] 버튼을 클릭한다
  Then /read 페이지로 이동한다
  And 텍스트가 문장별로 번역되어 표시된다
  And 핵심 표현이 ExpressionBar에 표시된다
```

### US-03: URL 입력으로 학습

```gherkin
Scenario: 사용자가 한국어 뉴스 URL을 입력한다
  Given 홈페이지 인풋이 보인다
  When URL을 붙여넣는다
  Then "Text will be fetched from URL" 안내가 표시된다
  When [Start] 버튼을 클릭한다
  Then /read?url={encoded} 페이지로 이동한다
  And 크롤링 → 분석 → 결과 표시 순서로 진행된다
```

### US-04: 문장 구조 분석

```gherkin
Scenario: 사용자가 어려운 문장의 구조를 분석한다
  Given /read 페이지에서 학습 중이다
  When 특정 문장의 [🔍] 버튼을 클릭한다
  Then 문장이 주어/목적어/서술어 등으로 파싱된다
  And 영어 어순으로 재배열한 읽기 순서가 표시된다
  And 문법 포인트 설명이 표시된다
```

### US-05: 단어 팝오버 조회

```gherkin
Scenario: 사용자가 모르는 단어를 선택한다
  Given /read 페이지에서 학습 중이다
  When 한국어 단어를 드래그하여 선택한다
  Then 팝오버가 표시된다
  And 뜻, 발음, 문맥 해석, 예문이 보인다
```

### US-06: 사이드바로 Watch 전환

```gherkin
Scenario: 사용자가 영상 분석으로 전환한다
  Given 홈페이지에서 Read 모드가 활성이다
  When 사이드바에서 "Watch"를 클릭한다
  Then /video 페이지로 이동한다
  And 사이드바 "Watch" 항목이 활성화된다
```

### US-07: 표현 복습 요약

```gherkin
Scenario: 사용자가 학습 후 표현을 복습한다
  Given /read 페이지에서 학습이 완료되었다
  When 페이지 하단으로 스크롤한다
  Then 카테고리별 표현 복습 요약이 표시된다 (Idiom, Collocation, Grammar Pattern 등)
  And [← New Article] 버튼이 보인다
  When 버튼을 클릭한다
  Then / (홈페이지)로 돌아간다
```

---

## 8. Functional Requirements

### 8.1 홈페이지 (`/`)

| ID | Requirement | Priority |
|----|------------|----------|
| FR-001 | 헤드라인(H1, 24px, 중앙) + 서브텍스트(14px, gray) 수직 중앙 정렬, 상단 여백 ~10vh | P0 |
| FR-002 | 텍스트에어리어 인풋: `max-width: 640px`, `border-radius: 12px`, Liner 스타일 | P0 |
| FR-003 | URL 패턴 감지 시 "Text will be fetched from URL" 안내 | P1 |
| FR-004 | [Start →] 버튼: 텍스트 → sessionStorage + `/read`, URL → `/read?url=` | P0 |
| FR-005 | Cmd/Ctrl+Enter 키보드 단축키로 제출 | P1 |
| FR-006 | "Recommended for you" 섹션 레이블 + 카드 그리드 (3/2/1열 반응형) | P0 |
| FR-007 | 카드: 난이도 배지 + 카테고리 + 제목(2줄) + 미리보기(2줄) + 읽기시간 + CTA | P0 |
| FR-008 | 카드 클릭 → `/read?article={articleId}` | P0 |
| FR-009 | 큐레이션 데이터: 최소 15개 학습 텍스트 (자체 제작/공공누리) | P0 |

### 8.2 읽기 학습 (`/read`)

| ID | Requirement | Priority |
|----|------------|----------|
| FR-010 | 3가지 입력 경로 지원: 텍스트(sessionStorage), URL(?url=), 카드(?article=) | P0 |
| FR-011 | ?article= 파라미터 → 정적 데이터에서 콘텐츠 로드 → 분석 시작 | P0 |
| FR-012 | 입력 없을 시 → 인풋 뷰 표시 (현재 /read의 인풋 모드 유지) | P0 |
| FR-013 | SSE 스트리밍 로딩 (crawling → analyzing → complete) | P0 |
| FR-014 | 결과: ExpressionBar + ArticlePanel + SentenceParser + SelectionPopover | P0 |
| FR-015 | ExpressionSummary 섹션: 결과 하단, 카테고리별 그룹핑 | P1 |
| FR-016 | [← New Article] 버튼: 결과 최하단, `/`로 이동 | P0 |
| FR-017 | [← Back] 헤더 버튼: `/`로 이동 | P0 |

### 8.3 사이드바

| ID | Requirement | Priority |
|----|------------|----------|
| FR-020 | "Articles" 메뉴 항목 삭제 | P0 |
| FR-021 | "Video Analysis" → "Watch"로 라벨 변경 | P0 |
| FR-022 | "Tools" 섹션 삭제, "Learn" 섹션 하나로 통합 | P0 |
| FR-023 | Read 활성 조건: `pathname === "/"` 또는 `/read` | P0 |
| FR-024 | Watch 활성 조건: `pathname === "/video"` 또는 `/analyze/*` | P0 |

### 8.4 모바일 사이드바 오버레이

| ID | Requirement | Priority |
|----|------------|----------|
| FR-030 | 오버레이에서 "Articles" 제거, "Watch" 라벨 적용 | P0 |

---

## 9. Non-Functional Requirements

### 9.1 Performance

| Metric | Target |
|--------|--------|
| LCP (Largest Contentful Paint) | < 2.5s |
| CLS (Cumulative Layout Shift) | < 0.1 |
| 카드 그리드 렌더링 | 정적 데이터이므로 즉시 |
| 번들 사이즈 변화 | Study 삭제 ≈ Article 확장 → 순감소 예상 |

### 9.2 Responsive

| Breakpoint | 카드 그리드 | 인풋 |
|------------|-----------|------|
| Desktop (>1024px) | 3열 | max-width: 640px |
| Tablet (640-1024px) | 2열 | max-width: 640px |
| Mobile (<640px) | 1열 | 전폭 (padding 16px) |

---

## 10. Technical Design

### 10.1 삭제 파일

| 파일 | 이유 |
|------|------|
| `features/study/components/study-input.tsx` | 홈페이지 인풋으로 대체 |
| `features/study/components/sentence-view.tsx` | ArticlePanel로 대체 |
| `features/study/components/view-mode-toggle.tsx` | ArticlePanel displayMode로 대체 |
| `features/study/components/expression-card.tsx` | ExpressionBar + Summary로 대체 |
| `features/study/components/recommended-links.tsx` | 카드 그리드로 대체 |
| `features/study/hooks/use-study-analysis.ts` | useArticleAnalysis로 대체 |
| `features/study/services/study.ts` | Article 서비스로 대체 |
| `features/study/types/study.ts` | Article 타입으로 통합 |
| `app/study/page.tsx` | /read로 통합 |
| `app/api/study/analyze/route.ts` | /api/article/stream으로 대체 |
| **총 10개 파일 삭제** | |

### 10.2 신규 파일

| 파일 | 역할 |
|------|------|
| `features/article/data/curated-articles.ts` | 정적 큐레이션 콘텐츠 (15-25개) |
| `features/article/components/expression-summary.tsx` | 표현 복습 요약 (Study에서 이식) |
| `features/article/components/article-card.tsx` | 추천 카드 컴포넌트 |
| **총 3개 파일 신규** | |

### 10.3 수정 파일

| 파일 | 변경 내용 |
|------|----------|
| `app/page.tsx` | Liner Hero 인풋 + 카드 그리드로 재작성 |
| `app/read/page.tsx` | ?article= 파라미터 지원, ExpressionSummary 추가, "New Article" 버튼 |
| `features/article/types/article.ts` | ExpressionCategory에 grammar_pattern, formal_expression, slang 추가 |
| `features/article/components/expression-bar.tsx` | 새 카테고리 라벨 추가 |
| `features/article/components/article-panel.tsx` | 변경 최소 (카테고리만) |
| `shared/components/layout/sidebar.tsx` | Articles 삭제, Watch 라벨, 섹션 통합 |
| `shared/components/layout/sidebar-overlay.tsx` | Articles 삭제, Watch 라벨 |
| **총 7개 파일 수정** | |

### 10.4 상태 관리 변화

```typescript
// 삭제: StudyInput의 sessionStorage 키
sessionStorage.removeItem("studyText");

// 신규: 홈페이지 인풋의 sessionStorage 키
sessionStorage.setItem("readText", text);

// /read 페이지 입력 우선순위
1. ?article={id} → curated-articles.ts에서 조회
2. ?url={encoded} → URL 크롤링
3. sessionStorage.readText → 텍스트 직접 분석
4. 없음 → 인풋 뷰 표시
```

---

## 11. Implementation Phases

### Phase 1: Feature 통합 + 사이드바 정리

- [ ] `features/article/types/article.ts` — ExpressionCategory 확장
- [ ] `features/article/components/expression-summary.tsx` — Study에서 이식
- [ ] `features/article/components/expression-bar.tsx` — 새 카테고리 라벨
- [ ] `app/read/page.tsx` — ?article= 지원, ExpressionSummary, "New Article" 버튼
- [ ] `shared/components/layout/sidebar.tsx` — Articles 삭제, Watch 라벨, 섹션 통합
- [ ] `shared/components/layout/sidebar-overlay.tsx` — Articles 삭제, Watch 라벨
- [ ] `features/study/` 전체 디렉토리 삭제
- [ ] `app/study/` 디렉토리 삭제
- [ ] `app/api/study/` 디렉토리 삭제
- [ ] 빌드 검증

**Deliverable**: Study/Article 통합 완료, 사이드바 Read+Watch 2개 메뉴

### Phase 2: 홈페이지 재설계

- [ ] `features/article/data/curated-articles.ts` — 큐레이션 콘텐츠 15-25개
- [ ] `features/article/components/article-card.tsx` — 카드 컴포넌트
- [ ] `app/page.tsx` — Liner Hero 인풋 + 카드 그리드로 재작성
- [ ] 반응형 점검 (3/2/1열 그리드)
- [ ] 빌드 검증

**Deliverable**: Liner Hero 홈페이지 + 추천 카드 그리드

### Phase 3: 정리 + 검증

- [ ] 불필요한 import/CSS 정리
- [ ] 모바일 레이아웃 점검
- [ ] 다크모드 점검
- [ ] `next build` 최종 빌드 검증
- [ ] 전체 라우트 동작 확인: `/`, `/read`, `/read?article=`, `/read?url=`, `/video`, `/analyze/[id]`

**Deliverable**: 빌드 검증 완료, 전체 플로우 동작

---

## 12. Success Metrics

| Metric | Before | Target | Measurement |
|--------|--------|--------|-------------|
| 사이드바 메뉴 수 | 3개 (Read, Articles, Video) | 2개 (Read, Watch) | 메뉴 카운트 |
| 중복 피처 | 2개 (Study + Article) | 1개 (Read) | 피처 디렉토리 수 |
| 첫 화면 → 학습 시작 | 텍스트 붙여넣기 필요 | 카드 클릭 1번 | UX 플로우 |
| 추천 콘텐츠 | 5개 텍스트 링크 | 15-25개 카드 | 콘텐츠 수 |
| 삭제 코드 | 0 | ~10개 파일 삭제 | 파일 수 |
| 빌드 성공 | ✅ | ✅ | next build |

---

## 13. Risks & Mitigations

| Risk | 확률 | 영향 | Mitigation |
|------|------|------|------------|
| Study 삭제 시 기존 /study URL 접근 | 중간 | 낮음 | /study 삭제 → 404. 외부 링크 없으므로 영향 미미 |
| 큐레이션 콘텐츠 15-25개 작성 부담 | 중간 | 중간 | 기존 추천 링크 5개 + GPT 초안으로 10-20개 추가 |
| /api/study/analyze 삭제 시 영향 | 낮음 | 낮음 | /api/article/stream이 동일 기능 대체 |
| ExpressionSummary 이식 시 스타일 차이 | 낮음 | 낮음 | Article 디자인 시스템에 맞게 조정 |

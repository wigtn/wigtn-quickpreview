# Korean Study App PRD — 한국어 학습 플랫폼

> **Version**: 1.1
> **Created**: 2026-02-13
> **Updated**: 2026-02-13 (Digging 리뷰 반영)
> **Status**: Draft
> **Related**: PRD_wigvu-v2.md (기존 영어 학습 플랫폼)

---

## 1. Overview

### 1.1 Problem Statement

한국어를 배우고 싶은 다양한 국가의 학습자들이 **실제 한국어 뉴스/기사를 교재로 활용**할 수 있는 플랫폼이 부족하다. 기존 한국어 학습 앱은 대부분 교과서식 문장 위주이며, 실제 뉴스나 기사를 라인 바이 라인으로 학습할 수 있는 도구가 없다.

### 1.2 Goals

- **G1**: 한국어 뉴스/기사 목록을 메인 페이지에 큐레이션하여 제공
- **G2**: 기사를 선택하면 한국어 원문을 읽을 수 있는 리딩 뷰 제공
- **G3**: 학습 모드 전환 시, 라인 바이 라인으로 원문(한국어) / 번역(사용자 모국어) 대조 뷰 제공
- **G4**: OpenAI API로 다국어 번역 지원 (영어, 일본어, 중국어, 스페인어, 베트남어 등)
- **G5**: GCP Cloud Run 기반 배포 (기존 인프라 활용)

### 1.3 Non-Goals (Out of Scope)

- 자체 뉴스 크롤링 서비스/봇 (Phase 1에서는 수동 큐레이션 또는 RSS)
- 발음/음성 학습 기능 (TTS)
- 실시간 채팅/튜터 기능
- 한국어 작문/문법 교정
- 단어장/복습 기능 (Phase 3에서 Supabase 도입 후)
- 유료 결제/프리미엄 기능
- 모바일 네이티브 앱

### 1.4 Scope

| 포함 | 제외 |
|------|------|
| 한국어 학습 텍스트 목록 (메인 페이지) | 자동 크롤링 봇 |
| 기사 원문 리딩 뷰 | PDF/이미지 OCR |
| 학습 모드 (라인 바이 라인 원문/번역) | 발음/음성 학습 |
| 사용자 언어 선택 (다국어 번역) | 문법 교정 |
| 숙어/표현 추출 | 사전 DB 구축 |
| 문장 구조 파싱 | 실시간 튜터 |
| SSE 스트리밍 진행률 | 로그인/회원 관리 (Phase 3) |
| 첫 방문 온보딩 (모국어/수준 선택) | 뉴스 매체 기사 전문 저장 |

### 1.5 콘텐츠 저작권 전략

> **중요**: 한국 뉴스 매체는 기사 전문 복사/가공에 민감하다. 상업적 서비스에서 기사 본문을 저장하거나 번역 결과를 캐싱하면 저작권 이슈가 발생할 수 있다.

| Phase | 전략 | 상세 |
|-------|------|------|
| Phase 1 | **자체 제작 + 공공 콘텐츠** | 학습용 샘플 텍스트 직접 작성, 공공누리 유형1(출처표시) 콘텐츠 활용 (정부 보도자료, 공공기관 발표문) |
| Phase 2 | **공공 RSS + 에버그린 콘텐츠** | 정부/공공기관 RSS 피드 자동 수집, 시의성 낮은 문화/에세이 콘텐츠 큐레이션 |
| Phase 3 | **매체 제휴** | 뉴스 매체 콘텐츠 제휴 협의, Creative Commons 라이선스 콘텐츠 |

**Phase 1 콘텐츠 원칙**:
- "뉴스 기사" 대신 **"학습 텍스트"**로 포지셔닝
- 자체 제작 또는 공공누리 콘텐츠만 사용
- URL 입력 기능: 기사 본문을 서버에 저장하지 않고 세션 내에서만 사용 후 폐기
- 원본 URL 링크를 항상 표시하여 출처 명확화

---

## 2. 기존 코드 분석 & 재활용 전략

### 2.1 현재 WIGVU 구조 요약

WIGVU는 **영어 학습 플랫폼** (한국인 대상)으로, 다음이 이미 구현되어 있다:

| 기능 | 상태 | 재활용 가능 여부 |
|------|------|----------------|
| YouTube 영상 분석 (STT, 번역, 요약) | 완료 | 간접 참고 |
| 영어→한국어 번역 (배치 처리) | 완료 | **핵심 재활용** (방향 반전) |
| SSE 스트리밍 진행률 | 완료 | **그대로 재활용** |
| 라인 바이 라인 자막/스크립트 표시 | 완료 | **구조 기반 재작성** |
| OpenAI GPT-4o-mini 연동 | 완료 | **그대로 재활용** |
| Next.js API Routes (BFF) | 완료 | **패턴 재활용** |
| FastAPI AI 서비스 | 완료 | **확장** |
| Cloud Run 배포 파이프라인 | 방금 완료 | **그대로 사용** |

### 2.2 기존 PRD(WIGVU v2)의 News Reader와 차이점

| 항목 | 기존 News Reader (PRD_wigvu-v2) | Korean Study (이 PRD) |
|------|------|------|
| **대상** | 한국인 영어 학습자 | 외국인 한국어 학습자 |
| **콘텐츠** | 영어 뉴스 기사 | 한국어 뉴스 기사 |
| **번역 방향** | 영어 → 한국어 | 한국어 → 사용자 모국어 (다국어) |
| **메인 페이지** | URL 직접 입력 | 기사 목록 큐레이션 |
| **입력 방식** | URL 또는 텍스트 붙여넣기 | 기사 카드 선택 (+ URL 입력) |
| **UI 언어** | 한국어 | 다국어 (영어 기본, i18n 확장) |

### 2.3 핵심 재활용 코드

#### 그대로 재활용 가능

| 파일 | 용도 |
|------|------|
| `apps/ai/app/services/translation.py` | 번역 서비스 — 번역 방향만 변경 |
| `apps/web/src/hooks/use-analysis-stream.ts` | SSE 스트리밍 패턴 |
| `apps/web/src/components/loading-state.tsx` | 로딩 상태 표시 |
| `apps/web/src/components/error-display.tsx` | 에러 표시 |
| `apps/web/src/components/ui/*` | UI 공통 컴포넌트 |
| `apps/ai/app/core/*` | 미들웨어, 에러 핸들링, Rate Limiter |
| `.github/workflows/deploy-cloudrun.yml` | Cloud Run 배포 |

#### 구조 기반 재작성 (참고하되 새로 작성)

| 기존 | 새 컴포넌트 | 변경점 |
|------|-----------|--------|
| `ScriptPanel` | `ArticleReadView` | 타임스탬프 제거, 읽기/학습 모드 토글 추가 |
| `KeyMomentsBar` | `ExpressionBar` | 영상 하이라이트 → 숙어/표현 카드 |
| `AnalysisView` | `StudyView` | YouTube 플레이어 제거, 기사 읽기 뷰로 교체 |
| `useAnalysisStream` | `useArticleStudy` | 이벤트 타입 변경 (crawl→translate→extract) |
| `UrlInput` | `ArticleInput` | URL 입력 + 기사 목록 선택 통합 |

### 2.4 리팩토링 권장 사항

현재 코드는 `components/`, `hooks/`, `lib/` 등이 플랫한 구조로 되어 있다. 기존 PRD_wigvu-v2.md의 Phase 1에서 이미 `features/` 기반 리팩토링을 계획했으므로, **이번 Korean Study 기능과 함께 리팩토링을 진행**하는 것이 효율적이다.

**권장 리팩토링 순서**:

```
1단계: shared/ 디렉토리 생성 → 공통 컴포넌트 이동
2단계: features/video/ 생성 → 기존 영상 분석 코드 이동
3단계: features/study/ 생성 → Korean Study 신규 코드
4단계: import 경로 전체 업데이트
```

---

## 3. Architecture

### 3.1 시스템 아키텍처 (2서비스 — Cloud Run)

```
┌─────────────────────────────────────────────────────────────┐
│                   Cloud Run (HTTPS/LB 자동)                  │
├──────────────────────────┬──────────────────────────────────┤
│                          │                                   │
│  ┌───────────────────┐   │   ┌───────────────────────────┐  │
│  │   wigvu-web       │   │   │       wigvu-ai            │  │
│  │   (Next.js 16)    │   │   │       (FastAPI)           │  │
│  │                   │   │   │                           │  │
│  │  Pages:           │   │   │  /api/v1/study/analyze    │  │
│  │   / (기사 목록)    │───┼──▶│  /api/v1/study/parse      │  │
│  │   /read/[id]      │   │   │  /api/v1/study/lookup     │  │
│  │   /analyze/[id]   │   │   │  /api/v1/translate        │  │
│  │                   │   │   │  /api/v1/analyze (기존)    │  │
│  │  API Routes:      │   │   │                           │  │
│  │   /api/study/*    │   │   │  OpenAI GPT-4o-mini       │  │
│  │   /api/analyze/*  │   │   └───────────────────────────┘  │
│  └───────────────────┘   │                                   │
│                          │                                   │
└──────────────────────────┴───────────────────────────────────┘
```

NestJS API Gateway는 제외 (Cloud Run 마이그레이션 결정 사항). Next.js API Routes가 BFF 역할 수행.

> **NestJS 의존성 처리 방침**:
> - **Korean Study 기능**: NestJS 의존 없이 **Web(Next.js API Routes) → AI(FastAPI) 직접 호출**로 구현
> - **기존 Video Analyzer**: 현재 Web의 SSE 코드가 NestJS 엔드포인트(`/youtube/metadata`, `/transcript`)를 호출 중. 이것은 별도 태스크로 AI 직접 호출 마이그레이션 진행 (이 PRD 범위 밖)
> - Cloud Run에는 Web + AI 2서비스만 배포. NestJS는 로컬 개발용으로만 유지

### 3.2 폴더 구조 (Target)

#### Web (Next.js)

```
apps/web/src/
├── app/
│   ├── layout.tsx
│   ├── page.tsx                       ← 리팩토링 (기사 목록 + 영상 분석 통합)
│   ├── globals.css
│   ├── read/[articleId]/
│   │   └── page.tsx                   ← 신규 (기사 읽기/학습 뷰)
│   ├── analyze/[videoId]/
│   │   └── page.tsx                   ← 유지 (기존 영상 분석)
│   └── api/
│       ├── study/                     ← 신규 (Korean Study BFF)
│       │   ├── analyze/route.ts       ← 기사 분석 (SSE 스트리밍)
│       │   ├── parse/route.ts         ← 문장 구조 파싱
│       │   └── lookup/route.ts        ← 단어/구문 조회
│       └── analyze/                   ← 유지 (기존 영상 분석)
│
├── features/
│   ├── study/                         ← 신규 (Korean Study)
│   │   ├── components/
│   │   │   ├── article-list.tsx       ← 기사 목록 카드
│   │   │   ├── article-card.tsx       ← 기사 카드 컴포넌트
│   │   │   ├── article-read-view.tsx  ← 원문 읽기 뷰
│   │   │   ├── study-view.tsx         ← 학습 모드 뷰 (라인 바이 라인)
│   │   │   ├── expression-bar.tsx     ← 숙어/표현 바
│   │   │   ├── sentence-parser.tsx    ← 문장 구조 분석 UI
│   │   │   ├── selection-popover.tsx  ← 단어 선택 팝오버
│   │   │   └── language-selector.tsx  ← 모국어 선택 드롭다운
│   │   ├── hooks/
│   │   │   ├── use-article-study.ts   ← SSE 스트리밍 학습 훅
│   │   │   └── use-text-selection.ts  ← 텍스트 선택 감지
│   │   ├── lib/
│   │   │   ├── study-service.ts       ← AI 서비스 호출
│   │   │   └── articles-data.ts       ← 기사 목록 데이터 (Phase 1: 정적)
│   │   └── types/
│   │       └── study.ts               ← 타입 정의
│   │
│   └── video/                         ← 기존 코드 이동
│       ├── components/                ← 기존 분석 관련 컴포넌트
│       ├── hooks/                     ← 기존 훅
│       ├── lib/                       ← 기존 서비스
│       └── types/                     ← 기존 타입
│
├── shared/                            ← 공통 코드
│   ├── components/
│   │   ├── ui/                        ← 기존 UI 컴포넌트
│   │   ├── navigation.tsx
│   │   ├── loading-state.tsx
│   │   ├── error-display.tsx
│   │   ├── error-boundary.tsx
│   │   ├── theme-toggle.tsx
│   │   └── providers.tsx
│   ├── lib/
│   │   ├── config/env.ts
│   │   ├── errors/
│   │   ├── logger.ts
│   │   └── utils.ts
│   └── types/
│       └── common.ts
```

#### AI (FastAPI)

```
apps/ai/app/
├── api/
│   ├── router.py                      ← 업데이트 (study 라우터 추가)
│   ├── health.py                      ← 유지
│   ├── study/                         ← 신규 (Korean Study)
│   │   ├── __init__.py
│   │   ├── analyze.py                 ← 기사 번역 + 표현 추출
│   │   ├── parse_sentence.py          ← 문장 구조 파싱
│   │   └── word_lookup.py             ← 단어/구문 조회
│   ├── analyze.py                     ← 유지 (기존 영상 분석)
│   ├── translate.py                   ← 유지 (기존 번역)
│   └── stt.py                         ← 유지 (기존 STT)
│
├── services/
│   ├── study/                         ← 신규
│   │   ├── __init__.py
│   │   ├── article_analyzer.py        ← 한국어 기사 분석 (번역+표현)
│   │   ├── sentence_parser.py         ← 문장 구조 분석
│   │   └── word_lookup.py             ← 단어 조회
│   ├── translation.py                 ← 유지 (번역 방향 파라미터화)
│   ├── llm.py                         ← 유지 (기존 영상 분석)
│   ├── stt_client.py                  ← 유지
│   └── youtube_audio.py               ← 유지
│
├── models/
│   ├── schemas.py                     ← 유지
│   └── study_schemas.py               ← 신규 (Korean Study 스키마)
│
└── core/                              ← 유지
```

---

## 4. User Stories

### US-001: 기사 목록 탐색

**As a** 한국어 학습자, **I want to** 메인 페이지에서 다양한 한국어 뉴스/기사 목록을 볼 수 있도록 **so that** 관심 있는 주제의 기사를 선택하여 학습할 수 있다.

```gherkin
Scenario: 메인 페이지 기사 목록 조회
  Given 사용자가 메인 페이지에 접속하면
  When 페이지가 로드될 때
  Then 카테고리별 한국어 뉴스/기사 카드가 표시되고
  And 각 카드에는 제목, 출처, 난이도, 카테고리가 표시된다
```

### US-002: 기사 원문 읽기

**As a** 한국어 학습자, **I want to** 기사를 선택하면 한국어 원문을 읽을 수 있도록 **so that** 실제 한국어 텍스트에 노출될 수 있다.

```gherkin
Scenario: 기사 원문 읽기
  Given 사용자가 기사 카드를 클릭하면
  When 기사 읽기 페이지(/read/[articleId])가 로드될 때
  Then 기사 제목, 출처, 날짜가 상단에 표시되고
  And 한국어 원문이 단락별로 표시된다
```

### US-003: 학습 모드 전환 (라인 바이 라인)

**As a** 한국어 학습자, **I want to** 학습 모드로 전환하면 원문(한국어)과 번역(내 모국어)을 라인 바이 라인으로 볼 수 있도록 **so that** 문장 단위로 대조하며 이해할 수 있다.

```gherkin
Scenario: 학습 모드 전환
  Given 사용자가 기사 읽기 페이지에서
  When "학습 모드" 버튼을 클릭하면
  Then 각 문장이 원문(한국어) / 번역(사용자 모국어) 쌍으로 표시되고
  And 번역은 SSE 스트리밍으로 실시간 진행률이 표시된다

Scenario: 모국어 선택
  Given 사용자가 처음 학습 모드를 사용할 때
  When 모국어를 선택하지 않았으면
  Then 모국어 선택 드롭다운이 표시되고
  And 선택한 언어가 localStorage에 저장된다
```

### US-004: 숙어/표현 자동 추출

**As a** 한국어 학습자, **I want to** 기사에 포함된 한국어 숙어/표현을 자동으로 볼 수 있도록 **so that** 교과서에 없는 실전 표현을 학습할 수 있다.

```gherkin
Scenario: 표현 추출 표시
  Given 학습 모드 분석이 완료되면
  When 학습 뷰가 로드될 때
  Then 기사에서 추출된 한국어 숙어/표현 목록이 상단에 표시되고
  And 각 표현에 뜻(사용자 모국어), 원문 내 사용 예시가 포함된다
```

### US-005: 문장 구조 파싱

**As a** 한국어 학습자, **I want to** 어려운 한국어 문장의 구조를 분석받을 수 있도록 **so that** 한국어 어순과 문법 구조를 이해할 수 있다.

```gherkin
Scenario: 문장 구조 분석 요청
  Given 학습 모드 뷰에서
  When 특정 문장의 "구조 분석" 버튼을 클릭하면
  Then AI가 해당 문장의 주어/서술어/목적어/조사/연결어미를 파싱하고
  And 사용자 모국어 어순으로 재배열한 읽기 순서가 표시되고
  And 한국어 문법 포인트가 사용자 모국어로 설명된다
```

### US-006: 단어/구문 팝오버

**As a** 한국어 학습자, **I want to** 모르는 한국어 단어나 구문을 드래그하면 즉시 뜻을 볼 수 있도록 **so that** 흐름을 끊지 않고 학습할 수 있다.

```gherkin
Scenario: 단어 선택 시 팝오버
  Given 기사 원문에서
  When 한국어 단어나 구문을 드래그/선택하면
  Then 팝오버가 표시되고
  And 선택한 텍스트의 뜻(사용자 모국어), 발음(로마자), 문맥 내 의미가 보인다
```

### US-007: 표시 모드 전환

**As a** 한국어 학습자, **I want to** 원문만/번역만/양쪽 보기를 전환할 수 있도록 **so that** 학습 수준에 맞게 조절할 수 있다.

```gherkin
Scenario: 표시 모드 토글
  Given 학습 모드에서
  When "원문만" 버튼을 클릭하면
  Then 번역이 숨겨지고 한국어 원문만 표시된다
```

---

## 5. Functional Requirements

### 5.1 온보딩 & 설정

| ID | Requirement | Priority | Dependencies |
|----|------------|----------|--------------|
| FR-A01 | 첫 방문 시 온보딩 모달 (모국어 선택 필수) | P0 | - |
| FR-A02 | 한국어 수준 선택 (초급/중급/고급) | P1 | FR-A01 |
| FR-A03 | 선택한 설정 localStorage 저장, 설정 페이지에서 변경 가능 | P0 | FR-A01 |
| FR-A04 | 텍스트 크기 조절 슬라이더 (학습 뷰) | P1 | - |

### 5.2 콘텐츠 목록 & 입력

| ID | Requirement | Priority | Dependencies |
|----|------------|----------|--------------|
| FR-001 | 메인 페이지에 한국어 학습 텍스트 목록을 카드 형태로 표시 | P0 | - |
| FR-002 | 카드에 제목, 출처, 카테고리, 난이도, 예상 읽기 시간 표시 | P0 | FR-001 |
| FR-003 | 카테고리 필터 (일상, 문화, 사회, 경제, 기술, 스포츠) | P1 | FR-001 |
| FR-004 | 난이도 필터 (초급/중급/고급) — 온보딩 수준 기반 기본값 | P1 | FR-001, FR-A02 |
| FR-005 | URL 직접 입력으로 임의의 한국어 기사 분석 (세션 내 사용, 미저장) | P1 | - |
| FR-006 | 텍스트 직접 붙여넣기 입력 | P1 | - |
| FR-007 | 기사 북마크 (localStorage 기반 즐겨찾기) | P2 | FR-001 |

### 5.3 기사 읽기

| ID | Requirement | Priority | Dependencies |
|----|------------|----------|--------------|
| FR-010 | 기사 원문(한국어) 리딩 뷰 | P0 | FR-001 |
| FR-011 | 기사 메타데이터 표시 (제목, 출처, 날짜, 카테고리) | P0 | FR-010 |
| FR-012 | 읽기 모드 ↔ 학습 모드 전환 토글 | P0 | FR-010 |
| FR-013 | 문장 호버 시 해당 줄 하이라이트 | P1 | FR-010 |

### 5.4 학습 모드 (번역 & 표시)

| ID | Requirement | Priority | Dependencies |
|----|------------|----------|--------------|
| FR-020 | 문장 단위 다국어 번역 (OpenAI, 한국어→사용자 모국어) | P0 | - |
| FR-021 | 라인 바이 라인 원문(한국어)/번역(사용자 모국어) 대조 뷰 | P0 | FR-020 |
| FR-022 | 사용자 모국어 선택 (온보딩에서 설정, 변경 가능) | P0 | FR-A01 |
| FR-023 | 표시 모드 토글 (양쪽/원문만/번역만) | P0 | FR-021 |
| FR-024 | SSE 스트리밍 진행률 (번역 진행 실시간 업데이트) | P1 | FR-020 |
| FR-025 | 한자어 병기 옵션 (일본어/중국어 사용자용 킬러 피처) | P2 | FR-020 |

> **FR-025 한자어 병기**: 일본어/중국어 사용자에게 한자를 보여주면 즉시 의미 파악 가능.
> 예: "한국은행(韓國銀行)은 기준금리(基準金利)를 동결(凍結)했다."
> OpenAI에 한자 병기 요청으로 자동 추출 가능. 추가 비용 미미.

### 5.5 숙어/표현 추출

| ID | Requirement | Priority | Dependencies |
|----|------------|----------|--------------|
| FR-030 | AI 기반 한국어 숙어/관용표현 자동 추출 (기사당 5-15개) | P0 | FR-020 |
| FR-031 | 각 표현에 사용자 모국어 뜻 + 원문 내 사용 위치 표시 | P0 | FR-030 |
| FR-032 | 표현 클릭 시 원문에서 해당 위치 하이라이트 | P1 | FR-031 |

### 5.6 문장 구조 파싱

| ID | Requirement | Priority | Dependencies |
|----|------------|----------|--------------|
| FR-040 | 문장별 "구조 분석" 버튼 | P0 | FR-021 |
| FR-041 | AI 한국어 문장 구조 파싱 (주어/서술어/목적어/보어/조사/연결어미) | P0 | FR-040 |
| FR-042 | 사용자 모국어 어순으로 재배열한 읽기 순서 표시 | P0 | FR-041 |
| FR-043 | 한국어 문법 포인트 설명 (조사, 연결어미, 존댓말 등) | P0 | FR-041 |
| FR-044 | 파싱 결과 접기/펼치기 | P1 | FR-040 |

### 5.7 단어/구문 팝오버

| ID | Requirement | Priority | Dependencies |
|----|------------|----------|--------------|
| FR-050 | 텍스트 선택 시 팝오버 표시 (Selection API) | P0 | - |
| FR-051 | 팝오버 내 사용자 모국어 뜻 표시 | P0 | FR-050 |
| FR-052 | 팝오버 내 "이 문장에서의 뜻" (문맥 해석) | P0 | FR-050 |
| FR-053 | 로마자 발음 표시 (Romanization) | P1 | FR-050 |

---

## 6. Non-Functional Requirements

### 6.1 Performance

| ID | Requirement | Target |
|----|------------|--------|
| NFR-001 | 메인 페이지 초기 로딩 (LCP) | < 2s |
| NFR-002 | 기사 분석(번역) 완료 (1000자 기준) | < 20s |
| NFR-003 | 팝오버 단어 조회 응답 | < 2s |
| NFR-004 | 문장 구조 파싱 응답 | < 5s |

### 6.2 Security

| ID | Requirement |
|----|------------|
| NFR-010 | OpenAI API Key는 서버에만 존재 (클라이언트 노출 금지) |
| NFR-011 | XSS 방지: 기사 HTML 렌더링 시 sanitize 처리 |
| NFR-012 | Rate Limiting 유지 (기존 설정) |
| NFR-013 | CORS 환경변수 기반 제어 (Phase 0에서 구현 완료) |

### 6.3 Accessibility

| ID | Requirement |
|----|------------|
| NFR-020 | 키보드 내비게이션 (Tab, Enter, Escape) |
| NFR-021 | 팝오버 Escape로 닫기 |
| NFR-022 | 적절한 폰트 크기 (본문 16px 이상) + 조절 가능 |
| NFR-023 | 다크/라이트 모드 지원 (시스템 설정 자동 감지) |
| NFR-024 | 화면 리더 기본 호환 (시맨틱 HTML, aria-label) |

### 6.4 Internationalization (i18n)

| ID | Requirement |
|----|------------|
| NFR-020 | UI 기본 언어: 영어 (국제 사용자 대상) |
| NFR-021 | 번역 지원 언어: 영어, 일본어, 중국어(간체), 스페인어, 베트남어, 태국어, 인도네시아어 |
| NFR-022 | 사용자 언어 선택 localStorage 유지 |

---

## 7. Technical Design

### 7.1 API Specification

#### `POST /api/study/analyze` (Next.js API Route → FastAPI)

**Description**: 한국어 기사 텍스트를 받아 문장 분리, 번역, 표현 추출

**Request Body**:
```json
{
  "text": "string (required) - 한국어 기사 본문",
  "title": "string (optional) - 기사 제목",
  "targetLanguage": "string (required) - 번역 대상 언어 코드 (en, ja, zh, es, vi, th, id)"
}
```

**Response**: SSE Stream
```
event: progress
data: {"step": "splitting", "message": "Splitting sentences...", "progress": 10}

event: progress
data: {"step": "translating", "message": "Translating... (3/15)", "progress": 40}

event: progress
data: {"step": "extracting", "message": "Extracting expressions...", "progress": 80}

event: result
data: {
  "sentences": [
    {
      "id": 0,
      "original": "연방준비제도는 금리를 동결했다.",
      "translated": "The Federal Reserve held interest rates steady."
    }
  ],
  "expressions": [
    {
      "expression": "금리를 동결하다",
      "meaning": "to freeze/hold interest rates",
      "category": "collocation",
      "sentenceId": 0,
      "context": "금리를 동결했다"
    }
  ],
  "meta": {
    "sentenceCount": 15,
    "expressionCount": 8,
    "targetLanguage": "en",
    "processingTime": 12500
  }
}

event: done
data: {}
```

**Error Responses**:

| Status | Code | Message |
|--------|------|---------|
| 400 | TEXT_REQUIRED | Text is required |
| 400 | TEXT_TOO_LONG | Text too long (15000 char limit) |
| 400 | UNSUPPORTED_LANGUAGE | Unsupported target language |
| 500 | ANALYSIS_FAILED | Analysis failed |

---

#### `POST /api/study/parse` (Next.js API Route → FastAPI)

**Description**: 한국어 문장의 문법 구조 분석

**Request Body**:
```json
{
  "sentence": "string (required) - 분석할 한국어 문장",
  "context": "string (optional) - 전후 문맥",
  "targetLanguage": "string (required) - 사용자 모국어 코드"
}
```

**Response 200 OK**:
```json
{
  "success": true,
  "data": {
    "components": [
      {
        "id": 0,
        "text": "연방준비제도는",
        "role": "Subject (주어)",
        "explanation": "The Federal Reserve",
        "particles": "는 (topic marker)"
      },
      {
        "id": 1,
        "text": "금리를",
        "role": "Object (목적어)",
        "explanation": "interest rates",
        "particles": "를 (object marker)"
      },
      {
        "id": 2,
        "text": "동결했다",
        "role": "Predicate (서술어)",
        "explanation": "froze / held steady",
        "particles": null
      }
    ],
    "readingOrder": "The Federal Reserve / interest rates / froze (held steady)",
    "grammarPoints": [
      {
        "type": "Topic Marker (은/는)",
        "explanation": "은/는 marks the topic of the sentence. Here, '연방준비제도' is what we're talking about.",
        "highlight": "는"
      },
      {
        "type": "Past Tense (-했다)",
        "explanation": "동결하다 (to freeze) + -했다 (past tense ending). This is a formal written style.",
        "highlight": "했다"
      }
    ]
  }
}
```

---

#### `POST /api/study/lookup` (Next.js API Route → FastAPI)

**Description**: 한국어 단어/구문의 뜻을 문맥과 함께 제공

**Request Body**:
```json
{
  "word": "string (required) - 조회할 한국어 단어/구문",
  "sentence": "string (required) - 해당 단어가 포함된 원문 문장",
  "targetLanguage": "string (required) - 사용자 모국어 코드"
}
```

**Response 200 OK**:
```json
{
  "success": true,
  "data": {
    "word": "동결",
    "romanization": "dong-gyeol",
    "meanings": [
      {"definition": "freeze; freezing", "partOfSpeech": "noun/verb stem"}
    ],
    "contextMeaning": "In this sentence, 동결 means 'to hold/freeze (rates)' — keeping something unchanged.",
    "examples": [
      "임금을 동결하다 (to freeze wages)",
      "자산 동결 (asset freeze)"
    ],
    "relatedWords": [
      {"word": "동결하다", "meaning": "to freeze (verb)"},
      {"word": "해동", "meaning": "thaw, unfreeze"}
    ]
  }
}
```

---

#### FastAPI AI 엔드포인트

| Endpoint | Description |
|----------|-------------|
| `POST /api/v1/study/analyze` | 한국어 기사 번역 + 표현 추출 (OpenAI) |
| `POST /api/v1/study/parse-sentence` | 한국어 문장 구조 파싱 (OpenAI) |
| `POST /api/v1/study/word-lookup` | 한국어 단어/구문 문맥 해석 (OpenAI) |

Web API Route → FastAPI 호출 시 `X-Internal-API-Key` 헤더로 인증 (기존 패턴 동일).

### 7.2 AI Prompts

> **다국어 프롬프트 원칙**:
> - 모든 프롬프트에 `IMPORTANT: All explanations MUST be written in {targetLanguage}.` 명시
> - 주요 언어(영/일/중)는 해당 언어로 시스템 프롬프트 작성하여 품질 향상
> - 기타 언어는 영어 기반 프롬프트 + targetLanguage 지정으로 처리
> - 응답 후 explanation 필드가 targetLanguage로 작성되었는지 후처리 검증

#### 번역 + 표현 추출 프롬프트

언어별 전용 프롬프트 (주요 3개 언어):

```python
STUDY_ANALYZE_PROMPTS = {
    "en": """System: You are a Korean language learning assistant for English-speaking learners.
IMPORTANT: All translations, meanings, and explanations MUST be in English.""",

    "ja": """System: あなたは日本語話者向けの韓国語学習アシスタントです。
重要: すべての翻訳、意味、説明は必ず日本語で書いてください。""",

    "zh": """System: 你是面向中文使用者的韩语学习助手。
重要: 所有翻译、含义和解释必须用中文书写。""",

    "default": """System: You are a Korean language learning assistant.
IMPORTANT: All translations, meanings, and explanations MUST be written in {targetLanguage}. The user does not understand Korean."""
}
```

공통 프롬프트 본문:

```
Analyze the given Korean article and return a JSON response with:

1. sentences: Each sentence with original Korean text and natural translation into {targetLanguage}
2. expressions: Korean idioms, collocations, and key vocabulary from the article (5-15 items)
   - expression: Korean expression
   - meaning: Translation in {targetLanguage}
   - category: "idiom" | "collocation" | "slang" | "formal_expression" | "grammar_pattern"
   - sentenceId: Sentence number where it appears (0-indexed)
   - context: The form used in the article

Focus on expressions that are:
- Commonly used in Korean but hard for foreigners to understand
- Different from literal word-by-word translation
- Important for understanding Korean news/media
```

#### 문장 구조 파싱 프롬프트

```
System: You are a Korean sentence structure analysis expert.
Help foreign Korean learners understand complex Korean sentences.
IMPORTANT: All role labels, explanations, and grammar points MUST be in {targetLanguage}.

Analyze the given Korean sentence into JSON format:

1. components: Breakdown by grammatical role
   - id: Unique number (from 0)
   - text: Original Korean text segment
   - role: Grammatical role in both Korean and {targetLanguage}
     (주어/Subject, 서술어/Predicate, 목적어/Object, 보어/Complement,
      부사어/Adverbial, 관형어/Modifier, 접속/Conjunction)
   - explanation: Translation of this component in {targetLanguage}
   - particles: Korean particles used (은/는, 이/가, 을/를, etc.) with brief explanation in {targetLanguage}

2. readingOrder: Rearranged in {targetLanguage} word order (separated by /)

3. grammarPoints: Key Korean grammar points for learners
   - type: Grammar point name in {targetLanguage}
   - explanation: Easy explanation in {targetLanguage}
   - highlight: The relevant Korean text
```

### 7.3 콘텐츠 데이터 소스 (Phase별)

| Phase | 방식 | 설명 |
|-------|------|------|
| Phase 1 | 정적 JSON | 자체 제작 학습 텍스트 + 공공누리 콘텐츠 (15-25개) |
| Phase 1.5 | 공공 RSS | 정부/공공기관 RSS 피드 자동 수집 (조기 도입) |
| Phase 2 | RSS 확장 | 공공 RSS + Creative Commons 콘텐츠 |
| Phase 3 | Supabase DB | 콘텐츠 저장, 캐싱, 사용자 히스토리 |

Phase 1 콘텐츠 구성 (시의성 낮은 에버그린 콘텐츠 위주):

| 카테고리 | 예시 | 난이도 | 수량 |
|---------|------|--------|------|
| 일상/생활 | 한국의 식문화, 대중교통, 날씨 | 초급 | 5-8개 |
| 문화/관광 | K-pop, K-drama, 관광지 소개 | 초급~중급 | 5-8개 |
| 사회/시사 | 교육 제도, 직장 문화, 인구 변화 | 중급 | 3-5개 |
| 경제/기술 | 한국 경제 동향, IT 산업 | 고급 | 3-4개 |

Phase 1 정적 데이터 구조:

```typescript
interface Article {
  id: string;
  title: string;
  source: string;           // "자체 제작", "정부 보도자료", "공공누리" 등
  sourceUrl?: string;        // 원본 URL (공공 콘텐츠의 경우)
  license?: string;          // "self" | "kogl-type1" | "cc-by" 등
  category: "daily" | "culture" | "society" | "economy" | "technology" | "sports";
  difficulty: "beginner" | "intermediate" | "advanced";
  readingTimeMinutes: number; // 예상 읽기 시간 (분)
  wordCount: number;          // 글자 수
  publishedAt: string;        // ISO 8601
  content: string;            // 한국어 본문
  imageUrl?: string;          // 썸네일
}
```

---

## 8. UI Wireframe

### 8.1 메인 페이지 (기사 목록)

```
┌──────────────────────────────────────────────────┐
│  WIGVU Korean Study          [EN ▼] [🌙]         │
├──────────────────────────────────────────────────┤
│                                                    │
│       Learn Korean with Real News                  │
│       실제 뉴스로 한국어를 배워보세요                │
│                                                    │
│  [All] [Politics] [Economy] [Culture] [Tech] ...  │
│  [All Levels] [Beginner] [Intermediate] [Advanced]│
│                                                    │
│  ┌─────────────────┐  ┌─────────────────┐        │
│  │ 📰 KBS          │  │ 📰 연합뉴스     │        │
│  │                  │  │                  │        │
│  │ 한국은행, 기준금리│  │ 서울시, 외국인   │        │
│  │ 동결 결정        │  │ 관광객 유치 확대  │        │
│  │                  │  │                  │        │
│  │ Economy · ⭐⭐    │  │ Society · ⭐     │        │
│  │ Intermediate     │  │ Beginner         │        │
│  └─────────────────┘  └─────────────────┘        │
│                                                    │
│  ┌─────────────────┐  ┌─────────────────┐        │
│  │ 📰 한겨레       │  │ 📰 조선일보      │        │
│  │ ...             │  │ ...              │        │
│  └─────────────────┘  └─────────────────┘        │
│                                                    │
│  ─── or paste a Korean article URL ───            │
│  ┌────────────────────────────────────────────┐   │
│  │  https://news.kbs.co.kr/...     [Analyze →]│   │
│  └────────────────────────────────────────────┘   │
│                                                    │
└──────────────────────────────────────────────────┘
```

### 8.2 기사 읽기 & 학습 뷰 (`/read/[articleId]`)

```
┌──────────────────────────────────────────────────┐
│  WIGVU    [← Back]              [EN ▼] [🌙]      │
├──────────────────────────────────────────────────┤
│  KBS News · 2026-02-13 · Economy                  │
│  "한국은행, 기준금리 동결 결정"                     │
├──────────────────────────────────────────────────┤
│  [📖 Reading Mode]  [📝 Study Mode]               │
│                                                    │
│  ── Study Mode 활성 시 ──────────────────────── │
│                                                    │
│  📌 Key Expressions (8)                            │
│  ┌──────────┐ ┌──────────┐ ┌───────────┐         │
│  │기준금리   │ │동결하다   │ │물가 상승   │ ...    │
│  │base rate  │ │to freeze │ │price rise  │         │
│  └──────────┘ └──────────┘ └───────────┘         │
│                                                    │
│  [Both] [Korean Only] [Translation Only]           │
│                                                    │
│  한국은행은 오늘 기준금리를 연 3.5%로 동결했다.     │
│  The Bank of Korea froze the base rate         [🔍]│
│  at 3.5% per year today.                           │
│  ─────────────────────────────────────────────── │
│  이번 결정은 최근 물가 상승세가 둔화된 점을         │
│  감안한 것으로 분석된다.                            │
│  This decision is analyzed as having taken     [🔍]│
│  into account the recent slowdown in price rises.  │
│  ─────────────────────────────────────────────── │
│                                                    │
│  ── 🔍 Structure Analysis (expanded) ──────────  │
│  │ [한국은행은]     Subject    The Bank of Korea   │
│  │ [오늘]          Adverb     today               │
│  │ [기준금리를]     Object     the base rate       │
│  │ [연 3.5%로]     Adverbial  at 3.5% per year    │
│  │ [동결했다]      Predicate  froze/held steady    │
│  │                                                 │
│  │ Reading order (English):                        │
│  │ The Bank of Korea / today / the base rate /     │
│  │ at 3.5% per year / froze                        │
│  │                                                 │
│  │ 💡 은/는 (Topic Marker): Marks "한국은행"       │
│  │    as the topic of this sentence                │
│  │ 💡 을/를 (Object Marker): "기준금리를"          │
│  │    marks what is being frozen                   │
│  └───────────────────────────────────────────── │
│                                                    │
│  ── Popover (단어 선택 시) ──                      │
│       ┌───────────────────┐                       │
│       │ 동결               │                       │
│       │ dong-gyeol         │                       │
│       │ freeze; freezing   │                       │
│       │                    │                       │
│       │ In this sentence:  │                       │
│       │ "held/froze (rates)"│                      │
│       └───────────────────┘                       │
│                                                    │
└──────────────────────────────────────────────────┘
```

---

## 9. Implementation Phases

### Phase 1: Core — 기사 목록 + 읽기 + 학습 모드

**목표**: 기사 선택 → 원문 읽기 → 학습 모드(라인 바이 라인 번역) 동작

| Step | Task | Files |
|------|------|-------|
| 1.1 | Web: `shared/` 디렉토리 생성, 공통 컴포넌트 이동 | ~15 파일 이동 |
| 1.2 | Web: `features/video/` 생성, 기존 영상 분석 코드 이동 | ~15 파일 이동 |
| 1.3 | Web: import 경로 전체 업데이트 | 영향받는 모든 파일 |
| 1.4 | Web: `features/study/types/study.ts` — 타입 정의 | 신규 1 파일 |
| 1.5 | Web: `features/study/lib/articles-data.ts` — 학습 텍스트 데이터 (15-25개, 자체 제작/공공누리) | 신규 1 파일 |
| 1.6 | Web: `features/study/components/onboarding-modal.tsx` — 첫 방문 온보딩 | 신규 1 파일 |
| 1.7 | Web: `features/study/components/article-list.tsx` — 콘텐츠 목록 | 신규 1 파일 |
| 1.8 | Web: `features/study/components/article-card.tsx` — 카드 (난이도, 읽기 시간 포함) | 신규 1 파일 |
| 1.9 | Web: `features/study/components/language-selector.tsx` — 모국어 선택 | 신규 1 파일 |
| 1.10 | Web: 메인 페이지 리팩토링 (학습 텍스트 목록 통합) | 기존 수정 |
| 1.11 | AI: `models/study_schemas.py` — 요청/응답 스키마 | 신규 1 파일 |
| 1.12 | AI: `services/study/article_analyzer.py` — 다국어 프롬프트 + 번역+표현 추출 | 신규 1 파일 |
| 1.13 | AI: `api/study/analyze.py` — 분석 엔드포인트 | 신규 1 파일 |
| 1.14 | Web: `features/study/lib/study-service.ts` — AI 서비스 호출 (NestJS 의존 없이 직접 호출) | 신규 1 파일 |
| 1.15 | Web: `features/study/hooks/use-article-study.ts` — SSE 스트리밍 훅 | 신규 1 파일 |
| 1.16 | Web: `app/api/study/analyze/route.ts` — BFF 엔드포인트 | 신규 1 파일 |
| 1.17 | Web: `features/study/components/article-read-view.tsx` — 원문 읽기 뷰 | 신규 1 파일 |
| 1.18 | Web: `features/study/components/study-view.tsx` — 학습 모드 뷰 | 신규 1 파일 |
| 1.19 | Web: `features/study/components/expression-bar.tsx` — 표현 바 | 신규 1 파일 |
| 1.20 | Web: `app/read/[articleId]/page.tsx` — 기사 페이지 | 신규 1 파일 |

**Deliverable**: 온보딩 → 콘텐츠 선택 → 원문 읽기 → 학습 모드 (라인 바이 라인 번역 + 표현 추출)

### Phase 2: Deep Learning — 구조 분석 + 팝오버

**목표**: 문장 구조 파싱 + 단어 팝오버

| Step | Task |
|------|------|
| 2.1 | AI: `services/study/sentence_parser.py` — 문장 구조 분석 |
| 2.2 | AI: `services/study/word_lookup.py` — 단어 조회 |
| 2.3 | AI: `api/study/parse_sentence.py`, `api/study/word_lookup.py` — 엔드포인트 |
| 2.4 | Web: `app/api/study/parse/route.ts`, `app/api/study/lookup/route.ts` — BFF |
| 2.5 | Web: `features/study/components/sentence-parser.tsx` — 구조 분석 UI |
| 2.6 | Web: `features/study/hooks/use-text-selection.ts` — 텍스트 선택 감지 |
| 2.7 | Web: `features/study/components/selection-popover.tsx` — 팝오버 UI |
| 2.8 | Web: 표시 모드 토글 (양쪽/원문만/번역만) |
| 2.9 | 반응형 모바일 최적화 |
| 2.10 | 주요 한국 매체 기사 5건 통합 테스트 |

**Deliverable**: 문장 구조 분석 + 단어 팝오버 + 표시 모드 전환

### Phase 3: Auth & Persistence (Supabase)

기존 PRD_wigvu-v2.md Phase 3과 동일 — Supabase Auth + DB

| Step | Task |
|------|------|
| 3.1 | Supabase 프로젝트 생성 & OAuth 설정 |
| 3.2 | 사용자 인증 (Google/GitHub SSO) |
| 3.3 | 단어장 기능 (팝오버 → DB 저장) |
| 3.4 | 학습 히스토리 |
| 3.5 | RSS 피드 기반 자동 기사 수집 |

---

## 10. Success Metrics

| Metric | Target | Measurement |
|--------|--------|-------------|
| 기사 분석(번역) 완료율 | 95%+ (에러 없이) | 에러 로그 |
| 번역 품질 (사용자 체감) | 자연스러운 번역 80%+ | 수동 검증 |
| 팝오버 응답 시간 | < 2s (p95) | 클라이언트 측정 |
| 구조 분석 정확도 | 문법 요소 올바른 분류 80%+ | 수동 검증 |
| 표현 추출 유용성 | 기사당 5-15개 유의미한 표현 | 수동 검증 |
| 지원 언어별 번역 품질 | 영어/일본어/중국어 우수, 기타 양호 | 수동 검증 |

---

## 11. Risks & Mitigations

| Risk | 확률 | 영향 | Mitigation |
|------|------|------|------------|
| 기사 저작권 이슈 | 높음 | 높음 | 자체 제작 + 공공누리 콘텐츠로 시작 (Section 1.5), 기사 본문 미저장 |
| NestJS 의존성 잔존 | 높음 | 중간 | Study는 AI 직접 호출로 독립 구현, Video는 별도 마이그레이션 |
| OpenAI 다국어 번역 품질 편차 | 중간 | 중간 | 언어별 전용 프롬프트 (Section 7.2), 주요 3개 언어 우선 |
| AI 응답 언어 불일치 | 중간 | 중간 | 프롬프트에 응답 언어 강제 + 후처리 검증 |
| 정적 콘텐츠 노후화 | 높음 | 중간 | 에버그린 콘텐츠 위주, RSS 조기 도입 (Phase 1.5) |
| 첫 사용자 이탈 | 중간 | 높음 | 온보딩 플로우 (FR-A01~A03) |
| 한국어 문장 분리 어려움 | 중간 | 낮음 | AI 기반 문장 분리 + 규칙 기반 fallback |
| OpenAI API 비용 증가 | 중간 | 중간 | 분석 결과 캐싱 (Phase 3), Rate Limiting |
| 프롬프트 일관성 | 중간 | 중간 | `response_format: json_object` 사용, 스키마 검증 |
| 폴더 구조 리팩토링 시 회귀 버그 | 중간 | 중간 | 단계적 이동, 각 단계 후 빌드 확인 |

---

## 12. 예상 비용

| 항목 | 월 예상 비용 |
|------|-------------|
| Cloud Run (AI, min=1) | ~$15-25 |
| Cloud Run (Web, scale-to-0) | ~$3-8 |
| OpenAI API (GPT-4o-mini) | ~$5-20 (사용량 비례) |
| Artifact Registry | ~$1-2 |
| **합계** | **~$24-55/월** |

---

## 13. Critical Files Reference

| File | Description |
|------|-------------|
| `apps/ai/app/services/translation.py` | 번역 서비스 — `source_language`, `target_language` 이미 지원 (그대로 활용) |
| `apps/ai/app/models/schemas.py` | TranslateRequest에 언어 필드 이미 존재 (그대로 활용) |
| `apps/web/src/hooks/use-analysis-stream.ts` | SSE 스트리밍 패턴 참고 원본 |
| `apps/web/src/components/script-panel.tsx` | StudyView 참고 원본 (both/original/translated 3모드 이미 구현) |
| `apps/web/src/components/key-moments-bar.tsx` | ExpressionBar 참고 원본 |
| `apps/ai/app/config.py` | AI 서비스 환경 설정 |
| `.github/workflows/deploy-cloudrun.yml` | Cloud Run 배포 파이프라인 |
| `docs/prd/PRD_wigvu-v2.md` | 기존 v2 PRD (참고) |
| `docs/prd/DIGGING_korean-study.md` | Digging 분석 보고서 |
